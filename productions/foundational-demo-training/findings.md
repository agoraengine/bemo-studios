# Findings: Foundational Demo Training Video

Product and canon findings collected during production. Raised, not fixed here.

## Canon (for bemo-os)

- **"No other tool does this" (beat 2 talk track).** The v3 demo script (`docs/people/affiliates/jon-kelley/foundational-demo.md`) carries "here's the part I want you to see, because no other tool does this" ahead of the gap-honesty line. It survived the founder edit of v3, so it appears accepted, but it is an absolute competitive claim that does not itself appear in the message map (the cleared line is "Generic AI memory can only see what's there. BeMo knows what's missing"). Worth either ratifying into the message map's competitive one-liners or softening in the demo script; the training video currently performs it as written. Flagged 2026-08-13 during the script's brand review.

## Website / launch (for Becky)

- **bemointel.ai still resolves to the old Wix site** (checked Aug 13, ~09:20): the root loads the Wix build and `/pricing` returns 404 there. The v3 demo script tells Jon to show `bemointel.ai/pricing` live on calls, and the new site's launch posture assumes the prototype build is what a prospect lands on. If the DNS cutover is scheduled for later today (memory has the site live "Aug 14" in one record and "went live Aug 13" in the demo doc), this resolves itself; if not, Jon demos a 404. Worth a check before his first call. S17 was captured from the local build (ce411c7) per the capture rule, so the video is unaffected.

## Product (for Lee)

- (none yet; the app capture pass will populate)
